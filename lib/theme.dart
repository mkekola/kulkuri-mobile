import 'package:flutter/material.dart';

// Mirrors the web app's "Signaali" theme tokens (style.css) - dark only for
// now, same as the map itself.
const bg = Color(0xff0a0f1c);
const surface = Color(0xff121a2c);
const surfaceTranslucent = Color(0xeb121a2c); // rgba(18,26,44,0.92)
const text = Color(0xffe9edf4);
const muted = Color(0xff8c96b3);
const lineStrong = Color(0x2ee9edf4); // rgba(233,237,244,0.18)
const hoverFill = Color(0x0de9edf4); // rgba(233,237,244,0.05)
const accentText = Color(0xffff7a45);
const onFill = Color(0xff0a0f1c);
